<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sudut kotak -->
    <corners android:radius="6dp" />

    <!-- Border -->
    <stroke
        android:width="1dp"
        android:color="#B0BEC5" /> <!-- abu2 soft -->

    <!-- Warna isi -->
    <solid android:color="#FFFFFF" />

    <!-- Padding dalam kotak -->
    <padding
        android:left="12dp"
        android:top="6dp"
        android:right="12dp"
        android:bottom="6dp" />
</shape>
